import React from "react";

const Menu = () => {
    return (
        <div className="Menu">
            <a href="#">Home</a>
            <a href="#">Categories</a>
            <a href="#">About us</a>
            <a href="#">Contacts</a>
        </div>
    );
}

export default Menu;